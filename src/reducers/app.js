import initialAppState from "../state/initialState";

function app(state = initialAppState, action) {
    switch (action.type)
    {
        default:
            return state
    }
}

export default app
